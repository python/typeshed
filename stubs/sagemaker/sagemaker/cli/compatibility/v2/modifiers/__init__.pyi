from sagemaker.cli.compatibility.v2.modifiers import (
    airflow as airflow,
    deprecated_params as deprecated_params,
    framework_version as framework_version,
    image_uris as image_uris,
    predictors as predictors,
    renamed_params as renamed_params,
    serde as serde,
    tf_legacy_mode as tf_legacy_mode,
    tfs as tfs,
    training_input as training_input,
    training_params as training_params,
)
