from collections.abc import Generator

from netaddr.ip import IPAddress

def valid_nmap_range(target_spec: str) -> bool: ...
def iter_nmap_range(*nmap_target_spec: str) -> Generator[IPAddress, None, None]: ...
