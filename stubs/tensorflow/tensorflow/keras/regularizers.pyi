from collections.abc import Callable
from typing import Any, overload
from typing_extensions import Self, TypeAlias

from tensorflow import Tensor

class Regularizer:
    def get_config(self) -> dict[str, Any]: ...
    @classmethod
    def from_config(cls, config: dict[str, Any]) -> Self: ...
    def __call__(self, x: Tensor, /) -> Tensor: ...

_Regularizer: TypeAlias = str | dict[str, Any] | Regularizer | None

@overload
def get(identifer: None) -> None: ...
@overload
def get(identifer: Callable[[Tensor], Tensor]) -> Callable[[Tensor], Tensor]: ...
@overload
def get(identifer: str | dict[str, Any]) -> Regularizer: ...
@overload
def get(identifer: _Regularizer) -> Regularizer | Callable[[Tensor], Tensor] | None: ...
def __getattr__(name: str) -> Any: ...
