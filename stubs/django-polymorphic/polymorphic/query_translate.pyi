from collections.abc import Sequence
from typing import Any

from django.db import models
from django.db.models import Q

def translate_polymorphic_filter_definitions_in_kwargs(
    queryset_model: type[models.Model], kwargs: dict[str, Any], using: str = ...
) -> list[Q]: ...
def translate_polymorphic_Q_object(
    queryset_model: type[models.Model], potential_q_object: Q | Any, using: str = ...
) -> Q | Any: ...
def translate_polymorphic_filter_definitions_in_args(
    queryset_model: type[models.Model], args: Sequence[Q], using: str = ...
) -> list[Q]: ...
def translate_polymorphic_field_path(queryset_model: type[models.Model], field_path: str) -> str: ...
def create_instanceof_q(
    modellist: type[models.Model] | Sequence[type[models.Model]], not_instance_of: bool = False, using: str = ...
) -> Q | None: ...
