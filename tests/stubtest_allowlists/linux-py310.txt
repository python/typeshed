_?curses.color_pair
(os|posix).setresgid
(os|posix).setresuid
(os|posix).sendfile
signal.sigtimedwait
signal.sigwaitinfo

# ==========
# Allowlist entries that cannot or should not be fixed
# ==========
